/*
 * HttpClient that makes it possible to request resources over Http.
 */

const request = require('request');

export interface HttpClientOptions {
}

export default class HttpClient {

  constructor(options: HttpClientOptions) {
  }
}